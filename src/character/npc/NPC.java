package src.character.npc;

import src.character.Character;

public class NPC extends Character {
    public NPC(String nombre, int healthPoints, int experience) {
        super(nombre, healthPoints, experience);
    }
}